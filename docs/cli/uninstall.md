---
summary: "CLI reference for `sendell uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `sendell uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
sendell uninstall
sendell uninstall --all --yes
sendell uninstall --dry-run
```

